apiVersion: v1
kind: Service
metadata:
  name: backend-network
spec:
  selector:
    apptype: backend-pod
  ports:
    - protocol: "TCP"
      port: 3001
      targetPort: 3000
  type: LoadBalancer
  